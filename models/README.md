Model files are downloaded on first run and gitignored.

- insightface buffalo_l -> %USERPROFILE%\.insightface\modelsuffalo_l\n- OpenCV YuNet + SFace -> this folder (python -m faceanchor fetch-models)
